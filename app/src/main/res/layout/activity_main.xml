<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/lt_main_root"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@android:color/white"
    tools:context=".MainActivity">

    <!-- FragmentContainerView რომ გვქონდეს FragmentA და FragmentB -->
    <androidx.fragment.app.FragmentContainerView
        android:id="@+id/lt_main_fragment_container"
        android:layout_width="0dp"
        android:layout_height="0dp"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        tools:layout="@layout/fragment_a" /> <!-- სტუდიოში კოდის დროს ლეიაუთში რომ უბრალოდ ჩანდეს პირველი გვერდი ანუ პირველი ფრაგმენტი -->

</androidx.constraintlayout.widget.ConstraintLayout>